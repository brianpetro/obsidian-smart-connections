- Fixed: transformers embedding model should fallback to non-GPU v4 usage and subsequently v3 if that still fails
- Improved: should only calculate connections results once (improves performance)
- Improved: update event listener to handle multiple embedded items

